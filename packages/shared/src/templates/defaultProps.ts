import { ITemplateProps, TTemplateKey } from '.';

const _defaultTemplateProps: Record<TTemplateKey, ITemplateProps> = {
  Admin: {},
  App: {},
  Default: {},
};

export default _defaultTemplateProps;
