{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/frontend/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna run develop --stream"
  }
}
