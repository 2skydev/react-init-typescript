{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/frontend/*"
  ],
  "devDependencies": {
    "@types/node": "^15.12.5",
    "@types/react": "^17.0.11",
    "@types/react-dom": "^17.0.8",
    "@typescript-eslint/eslint-plugin": "^4.28.1",
    "@typescript-eslint/parser": "^4.28.1",
    "eslint": "^7.29.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^3.4.0",
    "eslint-plugin-simple-import-sort": "^7.0.0",
    "lerna": "^4.0.0",
    "prettier": "^2.3.2",
    "typescript": "^4.3.5"
  },
  "scripts": {
    "start": "lerna run develop --stream --scope admin --scope client",
    "start:client": "lerna run develop --stream --scope client",
    "start:admin": "lerna run develop --stream --scope admin",
    "start:backend": "lerna run develop --stream --scope backend",
    "start:all": "lerna run develop --stream --scope admin --scope client --scope backend"
  }
}